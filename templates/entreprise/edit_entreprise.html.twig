{% extends 'base.html.twig' %}

{% block title %}Formuliare modifier !{% endblock %}

{% block body %}


<h1>Edit donnee</h1>
{{ form_start(form) }}
{{ form_widget(form) }}
    <button>{{ 'Update' }}</button>
{{ form_end(form) }}
<a href='{{ path('app_home') }}'><button>back to list </button></a>

{% endblock %}